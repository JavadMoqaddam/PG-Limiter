# ریویو انتشار — ۱۴ کامیت بعد از v1.3.1

**دامنه:** از `1033ee872ec14b248a902dedf9e8382a989e8fa5` (v1.3.1) تا `c4077c6`.
**تاریخ:** ۲۰۲۶-۰۸-۳۰ · **رأی فعلی: منتشر نکنید** (۳ بلاکر، همه با اصلاح کوچک قابل رفع)

**روش:** خواندن کد + داده‌ی واقعی پروداکشن. `Bash` در محیط ریویو با خطای seccomp از کار افتاده،
پس هیچ تست/بیلد/گیتی اجرا نشده. هر یافته با فایل و شماره‌ی خط قابل بازبینی است.
موارد علامت‌دار «تأیید پروداکشن» با دستور روی سرور خود اپراتور اثبات شده‌اند.

---

## بلاکرها

### B1 — یک خطای گذرای دیتابیس، سقف همه‌ی کاربران را ۲ می‌کند

مسیر کامل، هر خط خوانده شده:

1. [check_usage.py:749](utils/check_usage.py:749) متادیتای کاربران را می‌گیرد.
2. [check_usage.py:288](utils/check_usage.py:288) در صورت خطا فقط لاگ می‌کند و دیکشنری **ناقص** برمی‌گرداند.
3. [check_usage.py:811](utils/check_usage.py:811) — `if not user_meta.get("is_monitored", True)` — نبود متادیتا
   یعنی «مانیتور شود». حلقه روی همه‌ی کاربران دارای ترافیک می‌چرخد، نه فقط گروه‌های فیلترشده.
4. [check_usage.py:826](utils/check_usage.py:826) به `resolve_effective_limit` می‌رسد و در
   [check_usage.py:142](utils/check_usage.py:142) به `general_limit = 2` می‌افتد، چون نه
   `effective_ip_limit` هست نه `group_ids`.

**تأیید با کانفیگ اپراتور:** گروه‌های ۱۵ و ۱۷ سقف ۳ دارند → اشتباه بن می‌شوند.
`group_filter` فقط ۵ گروه را مانیتور می‌کند → بقیه‌ی کاربران (از ۲۸٬۴۱۴ ردیف `users`)
وارد enforcement می‌شوند. جهت خرابی «سخت‌گیرانه‌تر» است؛ برعکس اصل «کاربر به اشتباه بن نشود».

**اصلاح:** هر دو تابع batch شکست را اعلام کنند (`None`/raise) و سیکل enforcement را رد کند؛
یا حداقل کاربری که هیچ‌یک از سه منبع سقف برایش حل نشد کنار گذاشته شود، نه اینکه به ۲ بیفتد.

### B2 — آلودگی کش متادیتا با رکورد ناقص (بدون هیچ خطای دیتابیسی)

ترتیب فراخوانی در [check_usage.py:748-749](utils/check_usage.py:748): اول `get_group_limits_batch`،
بعد `get_active_users_metadata_batch`.

- [check_usage.py:199-201](utils/check_usage.py:199) و [220-222](utils/check_usage.py:220)
  رکوردی می‌نویسند که **فقط** `group_ids` دارد.
- [check_usage.py:250](utils/check_usage.py:250) حضور کلید در کش را «کامل بودن» تلقی می‌کند
  و دیگر از دیتابیس نمی‌پرسد.

نتیجه: هر کاربری که اول از مسیر group-limits دیده شود، تا ری‌فرش بعدی بدون `is_monitored`،
بدون `is_excepted` و بدون `effective_ip_limit` می‌ماند — همان مسیر B1، این بار بدون خطا.

**اصلاح:** `group_ids` در یک مپ جدا نوشته شود، یا رکورد «ناقص» علامت بخورد تا batch متادیتا
دوباره واکشی کند.

### B3 — سیستم مایگریشن روی ایمیج داکر هرگز کار نکرده (تأیید پروداکشن)

`start.sh` اول در مرحله‌ی ۳ `init_db()` را صدا می‌زند که در [database.py:164](db/database.py:164)
با `create_all` همه‌ی جدول‌ها را می‌سازد؛ بعد مرحله‌ی ۴ در [start.sh:187](start.sh:187)
`alembic upgrade head` را اجرا می‌کند که از `001_initial` شروع می‌شود و می‌خواهد `users` را بسازد
— که وجود دارد. خطا می‌خورد و [start.sh:194-196](start.sh:194) آن را می‌بلعد:

```
log_warn "Migrations skipped (may already be applied)"
log_debug "Details: ${MIGRATION_OUTPUT:0:100}..."
```

**سه شاهد روی دیتابیس زنده:** `alembic_version` خالی است (`[]`)؛ جدول‌های `admin_patterns` و
`limit_patterns` که مایگریشن ۰۰۶ باید حذف می‌کرد هنوز هستند.

**پیامد برای ریلیز:** هیچ مایگریشن جدیدی (از جمله اصلاح B4) اجرا نخواهد شد.

**اصلاح:** روی دیتابیسی که با `create_all` ساخته شده اول `alembic stamp head`، بعد `upgrade head`؛
و شکست مایگریشن باید بلند باشد و بوت را متوقف کند، نه یک `log_warn`.

---

## مهم (High)

### B4 — تاریخچه‌ی IP بی‌صدا نوشته نمی‌شود (تأیید پروداکشن)

- [models.py:283](db/models.py:283) محدودیت `UniqueConstraint("username","ip", name="uq_ip_history_username_ip")` را اعلام می‌کند.
- [001_initial.py:143](db/migrations/versions/001_initial.py:143) همان ایندکس را با `unique=False` می‌سازد و هیچ مایگریشنی محدودیت یونیک نمی‌سازد.
- کد **جدید** در [ip_history.py:112](db/crud/ip_history.py:112) روی همان جفت `on_conflict_do_update` می‌زند.
- [ip_history_tracker.py:54](utils/ip_history_tracker.py:54) استثنا را می‌گیرد، `warning` می‌نویسد و `0` برمی‌گرداند.

**تأیید:** روی دیتابیس اپراتور `uq_ip_history_username_ip` وجود ندارد و `ip_history` صفر ردیف دارد
(در ۱.۳.۱ تاریخچه در Redis بود و این جدول هرگز استفاده نشده). پس بعد از آپدیت، `bulk_record`
اولین نویسنده است و هر سیکل شکست می‌خورد؛ گزارش‌های ۲۴/۴۸ ساعته همیشه «هیچ کاربری عبور نکرد».

**اصلاح (بی‌ریسک، چون جدول خالی است):**
`CREATE UNIQUE INDEX uq_ip_history_username_ip ON ip_history(username, ip);`

### H1 — تصحیح شد: دو عدد لاگ دو جمعیت مختلف را می‌شمارند، نه دو الگوریتم متفاوت

**این یافته اولش اشتباه رتبه‌بندی شده بود و اینجا تصحیح می‌شود.** دو خط لاگ زیر در یک
سیکل با دو ثانیه فاصله ظاهر می‌شوند:

```
01:51:55 | Number of all active ips: 1590, devices: 1147
01:51:57 | 📊 Counting mode 'ip': 1379 devices from 1605 unique IPs
```

اختلاف از دو الگوریتم رقیب نمی‌آید. با خواندن کد مشخص شد:

- `enhanced_users_info` که مبنای عدد گزارش است، فقط کاربران **فیلترنشده** را دارد —
  [check_usage.py:466-467](utils/check_usage.py:466) `if email in filtered_users: continue`.
  با Group Filter شما که پنج گروه را include می‌کند، ۱۱۴۷ فقط کاربران همان پنج گروه است.
- پیش‌محاسبه‌ی enforcement در [check_usage.py:805-816](utils/check_usage.py:805) روی **همه‌ی**
  کاربران اسنپ‌شات می‌چرخد. پس ۱۳۷۹ جمعیت بزرگ‌تری است.
- `total_ips` در گزارش طول یک **مجموعه‌ی** سراسری IP است، در حالی که
  `total_ips_cycle` جمع تعداد IP هر کاربر است. ۱۵ عدد اختلاف یعنی ۱۵ IP بین دو کاربر
  مشترک بوده. هر دو درست‌اند.

برای یک کاربر معیّن، هر دو مسیر `count_devices` را روی **همان** آبجکت اسنپ‌شات و با همان
`device_config` صدا می‌زنند، پس عدد per-user یکی است. تنها واگرایی واقعی باقی‌مانده در
مسیر fallback است: وقتی کاربر هیچ جزئیات کانکشنی ندارد، گزارش
`len(user_info.formatted_ips)` را می‌شمارد ([check_usage.py:280-281](utils/check_usage.py:280))
که زیرشبکه‌های جمع‌شده را یک عدد حساب می‌کند («1.2.3.x (5)» = ۱)، در حالی که enforcement
`len(unique_ips)` را می‌گیرد (۵). در حالت `logs` هر IP ثبت‌شده یک کانکشن هم دارد، پس این
مسیر عملاً برای شما اجرا نمی‌شود.

**نتیجه:** بلاکر یا High نیست. تنها کار لازم، عوض کردن متن آن دو خط لاگ است تا معلوم شود
یکی «کاربران مانیتورشده» و دیگری «همه‌ی کاربران فعال» را می‌شمارد. آن بازآرایی پرخطرِ
انتقال پیش‌محاسبه به قبل از `check_ip_used` لازم نیست.

### H2 — نبود ایزولاسیون خطا: یک کاربر خراب، کل پراسس را می‌کشد

حلقه‌ی [check_usage.py:805](utils/check_usage.py:805) هیچ `try/except` ندارد و
`check_users_usage` مستقیم در [limiter.py:238](limiter.py:238) داخل TaskGroup await می‌شود.
بدتر: [check_usage.py:693](utils/check_usage.py:693) اسنپ‌شات IP را **قبل از** همه‌ی کارهای
خطاپذیر پاک می‌کند، پس شواهد آن سیکل هم می‌رود. چون `consecutive_violations` تعداد
فراخوانی‌های `add_warning` را می‌شمارد و **هیچ** مقایسه‌ای با تایم‌استمپ قبلی ندارد،
یک crash-loop پنجره‌ی ~۹ دقیقه‌ای را به ~۱-۲ دقیقه فشرده می‌کند.

**اصلاح:** بدنه‌ی حلقه در `try/except Exception` با `continue`؛ و یک گارد زمانی روی افزایش شمارنده.

**وضعیت: نیمه‌اصلاح شد.**

۱. سیکل دیگر پراسس را نمی‌کشد — فراخوانی `check_users_usage` در `run_check_users_usage`
   داخل `try/except Exception` رفت، با لاگ و ادامه دادن به `sleep`. یعنی یک کاربر خراب
   دیگر باعث `exit 1` و crash-loop نمی‌شود.

۲. گارد زمانی روی شمارنده اضافه شد، در
   [enhanced_system.py](utils/warning_system/enhanced_system.py) در شاخه‌ی رکورد موجودِ
   `add_warning`. شمارنده فقط وقتی بالا می‌رود که از اسکن قبلی دست‌کم
   `check_interval * 0.5` گذشته باشد؛ وگرنه رکورد تازه می‌شود اما تخلف شمرده نمی‌شود و یک
   WARNING ثبت می‌شود. زمان اسکن قبلی از خود `monitoring_end_time` بازیابی می‌شود
   (`monitoring_end_time - monitoring_period`)، پس هیچ فیلد جدیدی برای ذخیره لازم نشد —
   که مهم است چون این مقدار در `user_warnings.json` پرسیست می‌شود و باید از ری‌استارت
   هم عبور کند، و ری‌استارت دقیقاً همان حالتی است که می‌خواهیم بگیریم.

**آنچه انجام **نشد** و دلیلش:** ایزولاسیون واقعی per-user، یعنی `try/except` دور بدنه‌ی
حلقه‌ی ۱۳۰ خطی، مستلزم تغییر ایندنت همان ۱۳۰ خط است. بدون امکان اجرای پایتون در این محیط،
ایندنت دستی چنین بلوکی همان کاری است که باگ‌های خاموش می‌سازد. با اصلاح شماره‌ی ۱ بدترین
پیامد (crash-loop و فشرده شدن پنجره‌ی escalation) از بین رفته؛ باقی‌مانده این است که یک
استثنا هنوز سیکل را برای بقیه‌ی کاربران قطع می‌کند. این را با تست انجام دهید، نه کورکورانه.

### H3 — CI آن‌قدر که به نظر می‌رسد سبز نیست

- `ruff check`، `ruff format --check` و Bandit هر سه `continue-on-error: true` دارند:
  [ci.yml:49](.github/workflows/ci.yml:49)، [:54](.github/workflows/ci.yml:54)، [:159](.github/workflows/ci.yml:159).
- `python -m compileall` فقط `SyntaxError` می‌گیرد؛ ماژول‌ها را import نمی‌کند، پس یک نام گم‌شده
  بعد از تقسیم `settings.py` برای CI نامرئی است — یعنی پرخطرترین کلاس رگرسیون این ریفکتور.
- جاب مایگریشن `alembic upgrade head` را روی دیتابیس **خالی** اجرا می‌کند، پس سناریوی
  آپگرید نصب زنده هیچ‌جا تست نمی‌شود (که B3 و B4 هر دو از همین شکاف بیرون آمدند).
- [ci.yml:133](.github/workflows/ci.yml:133) فایل `tests/test_sse_logs.py` را از اجرا حذف می‌کند،
  در حالی که SSE منبع پیش‌فرض IP و مسیر داغ پروداکشن است. `core_test.py` هم دو بار
  اجرا نمی‌شود: هم `--ignore` شده، هم `pytest.ini` الگوی `test_*.py` دارد — و کامیت `c4077c6`
  همین فایل را تغییر داده، یعنی آن تغییر هرگز اجرا نشده.
- `pip install pytest pytest-asyncio pytest-cov` بدون پین ([ci.yml:125](.github/workflows/ci.yml:125))،
  پس سبز بودن بازتولیدپذیر نیست. فیکسچر `event_loop` با اسکوپ session در
  [conftest.py:20](tests/conftest.py:20) با `asyncio_default_fixture_loop_scope = function` در
  `pytest.ini` تناقض دارد — بعد از ریفکتور چرخه‌ی حیات لوپ، شکل غلطی است.

---

## متوسط

- **`init_db()` خطایش نادیده گرفته می‌شود** — [limiter.py:130](limiter.py:130). با SQLite به‌عنوان
  تنها استور، نتیجه ماشین «فقط بن» است: بن ادامه دارد ولی هیچ‌کس خودکار فعال نمی‌شود.
  باید با کد غیرصفر خارج شود.
- **معیار به شمارش خام IP برمی‌گردد** — [check_usage.py:769](utils/check_usage.py:769)
  `fallback_count=len(_u_ips)` برای کاربر بدون جزئیات کانکشن. همان معیاری که قبلاً
  ۴۵۰ مثبت کاذب ساخت. هر سه مسیر همین عدد را می‌بینند، پس ناسازگار نیست — ولی غلط است.
- **`violation_history.json` بدون ایمپورت‌کننده** — فایل ۴۴۹ کیلوبایتی زنده است و جدول SQLite هم
  ۲٬۷۸۴ ردیف دارد. تنها گیت ایمپورت در [start.sh:200](start.sh:200) دنبال
  `/app/.violation_history.json` است (با نقطه، در مسیر اشتباه) پس هرگز اجرا نمی‌شود.
- **لو رفتن بخشی از توکن بات** — [run_telegram.py:36](run_telegram.py:36) پانزده کاراکتر اول
  توکن را در سطح **INFO** لاگ می‌کند و [:29](run_telegram.py:29) بیست کاراکتر در DEBUG.
- **`run_telegram_bot()` هر شکست را می‌بلعد و نرمال برمی‌گردد** —
  [run_telegram.py:41](run_telegram.py:41) و [110-121](run_telegram.py:110). تسک بات در
  `limiter.py` داخل `_supervise` نیست، پس توکن اشتباه یا خطای شبکه = بات لال + بن فعال.
  گارد مرده‌ی [run_telegram.py:46](run_telegram.py:46) هم هنوز سر جایش است.
- **`VERSION = "1.3.1"`** در [limiter.py:27](limiter.py:27) بامپ نشده.
- **حذف کامیت‌نشده‌ی `tests/test_process_singletons.py`** — تستی که `05d3b97` برای محافظت از
  قرارداد «یک پراسس در هر عمر» اضافه کرده بود.
- **`_ensure_migrated()` قبل از موفقیت خودش را انجام‌شده علامت می‌زند** —
  [handel_dis_users.py:92](utils/handel_dis_users.py:92). با ری‌استارت جبران می‌شود.
- **`db_handler.py` وعده‌ی fallback ای می‌دهد که وجود ندارد** —
  [db_handler.py:26](utils/db_handler.py:26) لاگ می‌کند «falling back to JSON storage» ولی
  `init_db`/`get_db` هرگز bind نشده‌اند، پس فراخوانی بعدی `NameError` می‌دهد.
- **`CHECK_INTERVAL=60` در [.env.example:23](.env.example:23)** با ۱۸۰-۳۰۰ که واقعاً اجرا می‌شود نمی‌خواند.

---

## با کانفیگ این اپراتور منتفی شد

`device_count_mode = "ip"` است (نه `device`)، `cdn_nodes`/`cdn_inbounds` تنظیم نشده‌اند و
`subnet_grouping_mode = "/24"` است. بنابراین این موارد **مسیر مرده‌اند**:

- سه‌برابر شدن یک IP در حالت `ip` از طریق شاخه‌های CDN ([device_count.py:138-152](utils/device_count.py:138))
- سنتینل `"Unknown"` برای inbound و اجباری بودن `>>` در
  `INBOUND_REGEX` — چون در حالت `ip` اصلاً `inbound` در کلید نیست
- تطبیق حساس به حروف `cdn_inbounds`
- تقسیم گروه `/16` با ISP ناقص
- `node_id = None` که از شاخه‌های CDN رد می‌شود

جدول کلیدهای [device_count.py](utils/device_count.py) بازبینی شد و **با تصمیم نهایی اپراتور
مطابق است**: `node_id` در هیچ کلید بازگشتی نیست و فیکس `ae30c7d` سر جایش است.
قاعده‌ی ۳ اسکن هم در لاگ واقعی درست کار می‌کند (`new → updated → violation_limit_reached`).

مهاجرت کاربران دیسیبل هم سالم است: مسیر `/var/lib/pg-limiter/disable_users.json` در
[handel_dis_users.py:29](utils/handel_dis_users.py:29) مو‌به‌مو با پیش‌فرض ۱.۳.۱ یکی است،
هر سه شکل کلید قدیمی خوانده می‌شود، و ۴۶ کاربر دیسیبل فعلی در جدول `users` هستند.
دیتابیس هم روی ولوم پرسیست است ([Dockerfile:43](Dockerfile:43) + [docker-compose.yml:13](docker-compose.yml:13)).

---

## هنوز باز

- **گارد سلامتی SSE** (N1 پایین‌تر) — طراحی شده، پیاده نشده.
- **B3** — ترتیب `create_all` و `alembic` در `start.sh` و بلعیدن خطای مایگریشن.
- **B4** — ایندکس یونیک `ip_history`، که باید همراه اصلاح B3 برود وگرنه اجرا نمی‌شود.
- **H1** — یکی‌سازی عدد گزارش با عدد enforcement.
- **H2** — ایزولاسیون خطا در حلقه‌ی per-user و گارد زمانی روی افزایش شمارنده.

بررسی‌های در جریان (۴ ایجنت کوچک): احراز هویت `api_server`، پایداری وضعیت اخطار،
تأیید دکمه‌های مرده‌ی تلگرام، و دیف کامل مدل در برابر مایگریشن‌ها.

---

## یافته‌های خارج از دامنه‌ی ۱۴ کامیت (پیش‌از‌این هم بوده)

### SSE می‌تواند بی‌صدا خفه شود و شمارنده‌ها را پاک کند

[shared_state.py](utils/shared_state.py) هیچ سیگنال سلامتی ندارد — فقط `ACTIVE_USERS`، یک قفل و
دو تابع اسنپ‌شات. پس دیکشنری خالی عیناً یعنی «هیچ‌کس وصل نیست» و «خط لوله مرده».
و [get_logs.py:259](utils/get_logs.py:259) با `timeout=None` وصل می‌شود، پس یک اتصال half-open
هرگز استثنا نمی‌دهد؛ وضعیت نود روی `"✅ Connected"` ([:274](utils/get_logs.py:274)) می‌ماند و
صفر IP می‌دهد. بعد حلقه‌ی [check_usage.py:943](utils/check_usage.py:943) هر کاربر غایب را
`clear_user_trust_data` می‌کند، پس متخلف واقعی هرگز به اسکن سوم نمی‌رسد.

جهت خرابی کم‌بن‌کردن است نه بن اشتباه (از دست رفتن نود، شمارش را پایین می‌آورد).
`get_logs.py` در این ۱۴ کامیت تغییر نکرده — اما همین ریلیز در `c23ead3` برای حالت API
گارد `run_enforcement` گذاشت و برای `logs` که مسیر داغ است نگذاشت.

**اصلاح پیشنهادی:** تایم‌استمپ «آخرین رویداد» به‌ازای هر نود در `shared_state`، و رد کردن
دو حلقه‌ی پاک‌سازی وقتی هیچ نودی در بازه‌ی معقول رویدادی نداده. افزودن تایم‌اوت خواندن به
`httpx` درست نیست — استریم SSE ماهیتاً می‌تواند ساکت باشد.

**وضعیت: گارد جزئی اضافه شد.** در [check_usage.py](utils/check_usage.py) قبل از حلقه‌ی
پاک‌سازی، اگر اسنپ‌شات **خالی** باشد در حالی که کاربرانی زیر مانیتورینگ هستند، هیچ شمارنده‌ای
پاک نمی‌شود و یک ERROR ثبت می‌شود. این حالت «همه‌ی نودها مرده‌اند» را می‌گیرد — که همان
سناریویی است که کل بن‌ها را متوقف می‌کند — و به هیچ حالت جدیدی در ماژول‌های دیگر نیاز ندارد.

**آنچه نمی‌گیرد:** خرابی *جزئی*، یعنی یک نود از چهل‌و‌نه که نیمه‌مرده شده. برای آن هنوز
همان ضربان per-node لازم است، چون بدون آن «کاربر واقعاً قطع شد» از «نود کاربر مرده» قابل
تفکیک نیست. عمداً انجام نشد: نیاز به تغییر `shared_state.py` و `parse_logs.py` و
`check_usage.py` با هم دارد و بدون امکان اجرای تست، ارزش ریسکش را ندارد.

### تاریخچه‌ی تخلف در دو جا زندگی می‌کند و ادمین عدد اشتباه می‌بیند

مرجع واقعی escalation، SQLite است: [punishment_system.py:435](utils/punishment_system.py:435)
→ `get_violation_count_async` → [:228](utils/punishment_system.py:228) `ViolationHistoryCRUD.get_violation_count`.

اما سه مشکل تأییدشده:

1. **fallback خاموش:** [punishment_system.py:229-233](utils/punishment_system.py:229) هر استثنای
   دیتابیس را با `punishment_logger.debug` می‌گیرد و به `len(self.violations.get(username, []))`
   یعنی کپی JSON برمی‌گردد. در سطح لاگ عادی حتی دیده نمی‌شود. اگر JSON کهنه باشد،
   escalation بی‌صدا از پله‌ی ۱ (۱۰ دقیقه) شروع می‌شود، با وجود ۲٬۷۸۴ ردیف در SQLite.
2. **عددی که به ادمین نشان داده می‌شود از JSON می‌آید:**
   [punishment_system.py:366](utils/punishment_system.py:366) `violation_count` و
   `next_step_index` هر دو از `self.violations` خوانده می‌شوند، در حالی که تنبیه واقعی از
   SQLite می‌آید. پس «Next Punishment» در تلگرام می‌تواند با آنچه اعمال می‌شود متفاوت باشد.
3. **دستور نمایش تاریخچه خراب است:** [punishment.py:541](telegram_bot/handlers/punishment.py:541)
   کلید `v['time_ago']` را می‌خواند ولی [punishment_system.py:372-379](utils/punishment_system.py:372)
   فقط `step`، `duration` و `timestamp` می‌دهد. تابع `_format_time_ago` در
   [:382](utils/punishment_system.py:382) هست و هرگز استفاده نمی‌شود. نتیجه `KeyError` است که در
   [punishment.py:555](telegram_bot/handlers/punishment.py:555) گرفته می‌شود و ادمین
   `❌ Error: 'time_ago'` می‌بیند.

`punishment_system.py` در این ۱۴ کامیت تغییر نکرده، پس رگرسیون نیست — ولی هدف اعلام‌شده‌ی
ریفکتور حذف کپی‌های موازی بود و این یکی جا ماند، در حالی که `utils/db_handler.py` (که **در**
دامنه است) یک پوشش سوم و بی‌استفاده‌ی `DBViolationHistory` هم دارد.

**تله‌ی مهم:** `db/migrate_from_json.py` هنگام ایمپورت، تایم‌استمپ اصلی را دور می‌ریزد
([violations.py:34](db/crud/violations.py:34) `timestamp=time.time()`)، پس اگر کسی روزی گیت
ایمپورت را «درست» کند، همه‌ی تخلف‌های قدیمی «همین حالا» مهر می‌خورند و کاربران به آخرین
پله‌ی تنبیه می‌پرند. آن گیت در [start.sh:200](start.sh:200) به مسیر اشتباه اشاره می‌کند و
هرگز اجرا نمی‌شود — همین‌طور بگذارید تا وقتی تایم‌استمپ‌ها حفظ شوند.


### B1 — اصلاح شد (نوشته شده، اجرا نشده)

همه در [utils/check_usage.py](utils/check_usage.py):

1. کلاس `MetadataUnavailable` اضافه شد.
2. `get_group_limits_batch` و `get_active_users_metadata_batch` پارامتر `strict: bool = False`
   گرفتند؛ با `strict=True` خطای دیتابیس به‌جای «بلعیدن و برگرداندن دیکشنری ناقص»،
   `MetadataUnavailable` می‌دهد. مسیر گزارش (`check_ip_used`) دست‌نخورده ماند و همان
   رفتار قبلی را دارد، چون خرابی دیجست ایمنی‌بحرانی نیست.
3. `check_users_usage` این دو را با `strict=True` صدا می‌زند و اگر شکست خوردند
   **کل سیکل را رد می‌کند**: هیچ‌کس اخطار نمی‌گیرد، بن نمی‌شود و شمارنده‌اش پاک نمی‌شود.
4. کاربری که هیچ ردیفی در دیتابیس محلی ندارد دیگر با پیش‌فرض‌ها قضاوت نمی‌شود؛
   کنار گذاشته می‌شود و تعدادشان در یک خط WARNING دیده می‌شود.
5. مسیر «نرمال شد» هم برای کاربر بدون متادیتا پاک‌سازی نمی‌کند — سقفش نامعلوم است،
   پس «داخل محدوده» قابل اثبات نیست.

**تغییر رفتار که باید بعد از دیپلوی ببینید:** کاربرانی که هنوز به دیتابیس محلی سینک نشده‌اند
تا سینک بعدی enforcement نمی‌شوند. روی این نصب `users` حدود ۲۸ هزار ردیف دارد، پس اثر عملی
نزدیک صفر است؛ اگر آن خط WARNING عدد بزرگی نشان داد یعنی `user_sync` عقب افتاده.

### B2 — اصلاح شد (نوشته شده، اجرا نشده)

در [utils/check_usage.py](utils/check_usage.py):

1. هر رکورد تازه‌ای که `get_group_limits_batch` در `USER_METADATA_CACHE` می‌سازد با
   `{"_partial": True}` علامت می‌خورد — چون فقط `group_ids` دارد. اگر رکورد از قبل کامل
   بود دست‌نخورده می‌ماند و فقط `group_ids` به‌روز می‌شود.
2. `get_active_users_metadata_batch` رکورد علامت‌دار را **cache miss** حساب می‌کند، پس
   دیتابیس را می‌خواند و رکورد را یک‌جا با نسخه‌ی کامل جایگزین می‌کند (که علامت را پاک می‌کند).

هزینه‌اش یک کوئری اضافه برای هر کاربر، فقط یک‌بار، تا اولین خواندن کامل موفق.
با این تغییر اصلاح B1 هم کامل می‌شود: رکورد ناقص دیگر عضویت جعلی در `users_metadata_usage`
نمی‌سازد، پس یا کامل خوانده می‌شود یا کاربر کنار گذاشته می‌شود.

---

## مسیر آپدیت — `cmd_update` در pg-limiter.sh

کل تابع ۲۹ خط است ([pg-limiter.sh:567-595](pg-limiter.sh:567)) و پنج مشکل دارد که مستقیماً
به دیپلوی همین ریلیز مربوط‌اند:

1. **هیچ بکاپی از دیتابیس گرفته نمی‌شود.** بین خط ۵۷۸ و ۵۹۴ چیزی
   `/var/lib/pg-limiter/data/pg_limiter.db` را کپی نمی‌کند. تابع `cmd_backup` در همین اسکریپت
   وجود دارد اما فقط از `cmd_uninstall` صدا زده می‌شود. دیتابیس شما ۱۴ مگابایت با ۴۶ کاربر
   دیسیبل و ۲٬۷۸۴ ردیف تخلف است.
2. **اسکریپتِ در حال اجرا روی خودش بازنویسی می‌شود** —
   [pg-limiter.sh:582](pg-limiter.sh:582) `curl -sSL "$SCRIPT_URL" -o /usr/local/bin/pg-limiter`.
   بش اسکریپت را تنبل و تکه‌تکه می‌خواند، پس بازنویسی در جا می‌تواند باعث شود ادامه‌ی اجرا
   از یک آفست بایتی در فایل **جدید** برداشته شود. ضمناً `-f` ندارد، پس یک صفحه‌ی خطای HTML
   هم به‌عنوان CLI نصب می‌شود. هیچ چک‌سام یا امضایی هم بررسی نمی‌شود.
3. **فایل compose هرگز بازتولید نمی‌شود.** `create_compose_file` فقط از `cmd_install`
   صدا زده می‌شود. پس اگر compose نصب فعلی شما هنوز سرویس redis را اعلام کند، `--remove-orphans`
   آن را «یتیم» نمی‌بیند (چون در فایل هست) و `up -d` دوباره بالا می‌آوردش — یک redis که کد
   جدید هرگز استفاده نمی‌کند. و هر تغییر جدید compose در این ریلیز هم به شما نمی‌رسد.
4. **ایمیج پین نشده** — [pg-limiter.sh:15](pg-limiter.sh:15) روی `:latest` است. بعد از
   `docker pull` نسخه‌ی قبلی بی‌تگ می‌شود و مسیر رول‌بک ساده‌ای نمی‌ماند.
5. **پیام موفقیت بی‌قید و شرط چاپ می‌شود** — [pg-limiter.sh:594](pg-limiter.sh:594) بدون هیچ
   بررسی سلامت کانتینر یا نگاه به لاگ. در ترکیب با B3 (که خطای مایگریشن را به warning تبدیل
   می‌کند) اپراتور تیک سبز می‌گیرد حتی وقتی مایگریشن شکست خورده.

نکته‌ی مثبت: `down` بدون `-v` است، پس ولوم‌ها و دیتابیس سالم می‌مانند.

### چک‌لیست دیپلوی تا وقتی این موارد اصلاح نشده‌اند

```bash
# ۱. بکاپ، قبل از هر چیز
docker compose -f /etc/opt/pg-limiter/docker-compose.yml stop pg-limiter
cp -a /var/lib/pg-limiter/data/pg_limiter.db /root/pg_limiter.db.bak-$(date +%F)
cp -a /var/lib/pg-limiter/user_warnings.json /root/user_warnings.json.bak-$(date +%F)

# ۲. نسخه‌ی فعلی ایمیج را برای رول‌بک تگ کنید
docker tag ghcr.io/javadmoqaddam/pg-limiter:latest pg-limiter:rollback

# ۳. آپدیت
pg-limiter update

# ۴. تأیید — این سه خط را در لاگ بگردید
docker logs pg-limiter --tail 200 2>&1 | grep -iE "migration|alembic|Database initial"
docker logs pg-limiter --tail 200 2>&1 | grep -iE "skipped|⛔|⏭️"
docker logs pg-limiter --tail 200 2>&1 | grep -i "Counting mode"
```

---

## دور سوم — دیف کامل مدل در برابر مایگریشن‌ها

این مهم‌ترین سؤال باقی‌مانده برای تگ زدن بود: آیا شکاف دیگری از جنس `ip_history` هست؟
**پاسخ: نه، هیچ شکاف درستی‌ای نمانده.**

- **هیچ ستونی گم نیست.** همه‌ی ۲۷ ستون `users` را ۰۰۱/۰۰۲/۰۰۵ می‌سازند.
- **هیچ UNIQUE دیگری گم نیست.** تنها موردش `uq_ip_history_username_ip` بود که مایگریشن ۰۰۷
  همین ریلیز اضافه کرد.
- **هر سه upsert پشتوانه‌ی درست دارند:**
  `ip_history` روی (username, ip) → با ۰۰۷ بسته شد؛
  [users.py:585](db/crud/users.py:585) روی `username` → از ۰۰۱ یونیک است
  ([001_initial.py:39](db/migrations/versions/001_initial.py:39))؛
  و `ON CONFLICT(username)` خام در [database.py:99](db/database.py:99) همان ایندکس را دارد.
- هیچ `OR REPLACE` یا `INSERT OR IGNORE` در کل `db/` و `utils/` نیست.

اما **هفت ایندکس** فقط در `db/models.py` وجود دارند و هیچ مایگریشنی نساخته‌شان. روی نصب شما
که ارتقا یافته (نه تازه‌ساخته)، هیچ‌کدام موجود نیست:

| ایندکس | جدول | هزینه‌ی نبودش |
|---|---|---|
| `ix_users_owner_username` | users | [users.py:141](db/crud/users.py:141) `get_by_owner_username` → اسکن کامل |
| `ix_users_special_limit` | users | [users.py:347](db/crud/users.py:347) `special_limit IS NOT NULL` → اسکن کامل |
| `ix_users_status_disabled` | users | جاروی دیسیبل/فعال‌سازی بدون ایندکس ترکیبی |
| `ix_subnet_isp_cached_at` | subnet_isp | [subnet_isp.py:131](db/crud/subnet_isp.py:131) `cleanup_old` → اسکن کامل |
| `ix_violation_username_timestamp` | violation_history | [violations.py:53](db/crud/violations.py:53) پنجره‌ی تخلف per-user |
| `ix_ip_history_username` | ip_history | تقریباً با ایندکس ترکیبی پوشیده — اثر کم |
| `ix_ip_history_username_last_seen` | ip_history | [ip_history.py:63](db/crud/ip_history.py:63) گزارش ۲۴/۴۸ ساعته |

**اصلاح شد:** [008_missing_indexes.py](db/migrations/versions/008_missing_indexes.py) هر هفت را
می‌سازد، هر کدام فقط اگر نبود. عمداً مایگریشن جداگانه از ۰۰۷ است: آن یکی یک باگ درستی
(upsert شکسته) را می‌بست، اینها فقط زمان کوئری‌اند. روی ۲۸ هزار ردیف `users` ساختنشان
کسری از ثانیه است و قبل از شروع enforcement در بالا آمدن کانتینر اجرا می‌شود.

---

## H3 — CI که سبز بود ولی چیزی را ثابت نمی‌کرد

سه ایراد ساختاری داشت، هر سه اصلاح شد.

### ۱. جاب مایگریشن هیچ‌وقت مایگریشن را روی دیتابیس پُر اجرا نمی‌کرد

این دقیقاً دلیل ندیده‌شدن B3 و B4 است. جاب قبلی دو مرحله داشت که **به دو فایل متفاوت**
کار می‌کردند: `create_all` روی `./data/test_ci.db` می‌ساخت، بعد `alembic upgrade head`
سراغ مقدار `alembic.ini` یعنی `./data/pg_limiter.db` می‌رفت
([alembic.ini:53](alembic.ini:53)) — یک فایل خالی دیگر. پس هیچ سناریویی مایگریشن را روی
دیتابیسی که از قبل جدول داشت اجرا نمی‌کرد، و تنها نوع دیتابیسی که در پروداکشن وجود دارد
همین است.

جاب جدید پنج سناریو دارد و هر کدام با `DATABASE_URL` دیتابیس خودش را نام می‌برد
(که [env.py:24-26](db/migrations/env.py:24) رعایتش می‌کند):

1. دیتابیس خالی → `upgrade head` → تأیید رسیدن به head.
2. **تطابق اسکیمای مهاجرت‌شده با `db/models.py`** — جدول، ستون و گروه‌های UNIQUE.
3. دیتابیس `create_all` → `stamp head` → `upgrade head` (همان مسیری که `start.sh` می‌رود).
4. دیتابیس ۰۰۱..۰۰۶ با ردیف‌های **تکراری** `ip_history` → `upgrade head` → تأیید اینکه ۰۰۷
   دوتایی‌ها را روی جدیدترین ردیف جمع کرد.
5. خطی بودن زنجیره (یک head) و سالم بودن `downgrade -1`.

سناریو ۲ و ۴ در پایان همان دستور `ON CONFLICT (username, ip)` را که `bulk_record` می‌فرستد
روی فایل واقعی اجرا می‌کنند. اگر ایندکس یونیک نباشد، SQLite همان خطای پروداکشن را می‌دهد و
جاب سرخ می‌شود. ابزارش [tools/ci_migration_checks.py](tools/ci_migration_checks.py) است و
روی لپ‌تاپ هم قابل اجراست.

نکته‌ی طراحی: نبودن ایندکس ساده در تست تطابق **warning** است نه خطا. ایندکس گم‌شده هزینه‌ی
زمان است نه درستی، و یک ریلیز نباید سر یک اسکن جدولی متوقف شود. جدول، ستون و UNIQUE خطای
قطعی‌اند.

### ۲. تنها چک سراسری کد، `compileall` بود

`compileall` فقط ثابت می‌کند هر فایل پارس می‌شود. دربارهٔ نامی که دیگر وجود ندارد ساکت است —
و این همان چیزی است که یک ریفکتور بزرگ می‌شکند: بعد از تغییر نام، همه‌ی صداکننده‌های قدیمی
هنوز بی‌عیب کامپایل می‌شوند. برای یک حلقه‌ی پس‌زمینه ممکن است ساعت‌ها بعد از دیپلوی معلوم شود
و برای یک هندلر تلگرام، اولین باری که کسی دکمه را می‌زند.

دو چک اضافه شد:

- **[tests/test_imports.py](tests/test_imports.py)** — همه‌ی ماژول‌های `api/`, `cli/`, `db/`,
  `telegram_bot/`, `utils/` و چهار اسکریپت ریشه را ایمپورت می‌کند. `ModuleNotFoundError` برای
  پکیج شخص ثالثِ نصب‌نشده رد می‌شود (پس روی لپ‌تاپ بدون requirements هم قابل اجراست) ولی
  `ImportError`/`NameError`/`AttributeError` و هر `ModuleNotFoundError` که نام یکی از پکیج‌های
  خودمان را ببرد، تست را می‌شکند. `sys.argv` موقتاً خالی می‌شود چون `limiter.py` هنگام ایمپورت
  `parse_args()` صدا می‌زند.
- **یک مرحله‌ی مسدودکننده‌ی ruff** فقط با `E9,F63,F7,F82` — یعنی نام تعریف‌نشده و سینتکس
  واقعاً خراب. سلیقه‌ای نیست، پس مسدود می‌کند. اجرای کامل ruff و `ruff format` همچنان
  advisory ماندند: ایراد استایل نباید بین یک اصلاح تأییدشده و پروداکشن بایستد.

**هشدار صادقانه:** من نمی‌توانم در این محیط ruff را اجرا کنم (Bash خراب است). `F821` از قبل
هم در اجرای advisory فعال بود، پس اگر جایی در کد نام تعریف‌نشده وجود داشته باشد، این مرحله‌ی
جدید CI را سرخ می‌کند. اگر سرخ شد، **چیز واقعی پیدا کرده** — نه اینکه چک بدتنظیم باشد.

### ۳. لیست `--ignore` و پین‌نبودن ابزار تست

- `core_test.py` با الگوی `test_*.py` هیچ‌وقت جمع نمی‌شد؛ بودنش در لیست فقط لیست را
  کامل‌تر از آنچه بود نشان می‌داد.
- `test_new_api.py` و `test_sse_logs.py` به کردنشیال واقعی پنل نیاز دارند. الان با
  `pytest.mark.integration` علامت خورده‌اند و [conftest.py:155](tests/conftest.py:155) بدون
  `--run-integration` ردشان می‌کند — جمع می‌شوند و صادقانه skip می‌شوند، نه اینکه پنهان بمانند.
- `test_minimal.py` کلاس‌های ماک تعریف‌شده در خودش را تست می‌کند و هیچ assert ندارد؛ نه می‌تواند
  بشکند و نه کمکی می‌کند. در لیست ماند، این بار با توضیح.
- `pytest`/`pytest-asyncio`/`pytest-cov` پین شدند. `pytest.ini` به
  `asyncio_default_fixture_loop_scope` (نیازمند pytest-asyncio ≥ 0.24) و `asyncio_mode = auto`
  تکیه دارد؛ نصب بدون پین می‌تواند نسخه‌ی major جدیدی بیاورد که نحوه‌ی جمع‌آوری تست async را
  عوض کند، و حالت خرابی‌اش بی‌صداست: همه‌ی تست‌های async از اجرا می‌افتند و جاب همان سبز
  می‌ماند.
- فیکسچر `event_loop` با scope=session از `conftest.py` حذف شد. pytest-asyncio آن را در ۰.۲۳
  deprecate و در ۱.۰ حذف کرد، و کنار `asyncio_default_fixture_loop_scope = function` دقیقاً
  همان ترکیبی است که یا در جمع‌آوری خطا می‌دهد یا تست‌ها را روی لوپ بسته می‌گذارد.
- اسکن Bandit همچنان advisory است ولی نتیجه‌اش به‌جای گم شدن در لاگِ مرحله‌ای که «اجازه‌ی
  شکست دارد»، در صفحه‌ی summary همان ران نوشته می‌شود.

---

## نسخه — به ۱.۴.۰ رسید

`1.4.0` و نه `1.3.2`: ردیس حذف شده و SQLite تنها انبار شده، اسکیما زیر مدیریت Alembic رفته،
منبع IP قابل سوییچ شده و رفتار enforcement تغییر کرده. هیچ‌کدام patch نیست.

نسخه در **شش جا** به‌صورت رشته‌ی خام تکرار شده و هیچ منبع یگانه‌ای ندارد. همه‌ی شش‌تا عوض شد:

| فایل | خط | چه چیزی می‌خواندش |
|---|---|---|
| [limiter.py](limiter.py:27) | 27 | `--version`، بنر استارتاپ، و **رجکس `release.yml`** |
| [pg-limiter.sh](pg-limiter.sh:7) | 7 | `cmd_help` و `pg-limiter version` |
| [start.sh](start.sh:3) | 3 | بنر بوت کانتینر |
| [cli_main.py](cli_main.py:178) | 178 | دستور `version` در CLI |
| [api_server.py](api_server.py:158) | 158 | متادیتای OpenAPI/Swagger |
| [README.md](README.md:6) | 6 | بَج مستندات |

`limiter.py` منبع مؤثر است چون [release.yml:23-24](.github/workflows/release.yml:23) وقتی تگ
`v*` پوش نشده باشد با رجکس از همان فایل نسخه را درمی‌آورد. عمداً به یک `version.py` مشترک
منتقل نشد: رجکس ریلیز به `limiter.py` گره خورده و دو اسکریپت بش هم به هر حال کپی خودشان را
لازم دارند، پس ریفکتورش بلافاصله قبل از تگ زدن ریسک بی‌دلیل است. لیست بالا برای بار بعد
مکانیکی است.

هیچ CHANGELOG در ریپو نیست.

**نکته‌ی مهم برای دیپلوی:** هیچ‌جای کد نسخه‌ی محلی را با نسخه‌ی ریموت مقایسه نمی‌کند.
`cmd_update` در [pg-limiter.sh:567-595](pg-limiter.sh:567) بی‌قید و شرط اسکریپت را دوباره
دانلود می‌کند و روی `:latest` یک `docker pull` می‌زند؛ نه نسخه‌ای می‌خواند و نه مقایسه‌ای
می‌کند. ربات تلگرام هم هیچ مسیر آپدیتی ندارد — نه `CommandHandler` برای `update`، نه ورودی
در `CALLBACK_ROUTES`، نه ماژول updater. پس بالا رفتن این عدد به‌خودی‌خود هیچ‌جا اعلام
نمی‌شود؛ تنها راه دیدنش لاگ بوت کانتینر یا `pg-limiter version` است.

---

## دور چهارم — آدیت کامل‌بودن

یک ایجنت مستقل کل این فایل را خواند و هر ادعا را در برابر کد فعلی چک کرد (بدون اعتماد به
برچسب‌های «اصلاح شد» خودِ فایل). چهار مورد واقعاً باز مانده بود؛ همه اصلاح شد.

### ۱. لو رفتن توکن بات در `start.sh` — همان باگ، جای دیگر

[start.sh:94](start.sh:94) دقیقاً همان `${BOT_TOKEN:0:15}` را داشت که در `run_telegram.py`
اصلاحش کردیم. توکن ساختار `<۱۰ رقم>:<۳۵ کاراکتر راز>` دارد، پس ۱۵ کاراکتر اول = چهار
کاراکتر از راز، در هر بوت. حالا `id=${BOT_TOKEN%%:*}` است و راز اصلاً چاپ نمی‌شود.

### ۲. `tests/test_process_singletons.py` برگشت

حذفش uncommitted بود، پس از `main` بازیابی شد. این تست قرارداد «یک پراسس، یک شیء» را قفل
می‌کند: اینکه صف dispatcher و صف کاربران ناشناس با عوض شدن event loop بازساخته **نشوند** —
همان چیزی که باعث می‌شد بک‌لاگ پیام‌ها و کاربران منتظر lookup بی‌صدا دور ریخته شوند.

### ۳. معیار fallback شمارش دستگاه — دو مسیر، دو عدد

این تنها مورد باقی‌مانده از H1 بود و واقعی است:

- enforcement در [check_usage.py](utils/check_usage.py) `fallback_count=len(_u_ips)` می‌داد —
  شمارش خام IP، که subnet grouping را نادیده می‌گیرد و یک /24 کامل را N دستگاه می‌خواند.
  همان معیاری که موج قبلی ~۴۵۰ مثبت کاذب را ساخت.
- گزارش در [device_count.py](utils/device_count.py) `len(user_info.formatted_ips)` می‌داد —
  رشته‌های نمایشی، که «1.2.3.x (5)» را ۱ می‌شمارد.

هیچ‌کدام با عددی که مسیر کامل (`device_key`) می‌داد یکی نبود. تابع جدید
`count_devices_from_ips` همان قاعده‌ی subnet را روی آدرس‌های خالی اعمال می‌کند و هر دو مسیر
از آن عبور می‌کنند. چون inbound نامعلوم است، عدد در هر دو حالت شمارش یکی است و یک کران
پایین است — جهت ایمن، چون هرگز تخلفی نمی‌سازد که مسیر کامل پیدا نمی‌کرد.

در حالت `logs` هر IP یک کانکشن هم دارد، پس این مسیر برای این نصب عملاً اجرا نمی‌شود؛
یعنی اصلاحش کم‌ریسک است ولی باگ نهفته را می‌بندد. هفت تست برای تابع جدید و دو تست برای
شاخه‌ی fallback گزارش اضافه شد.

---

## دور پنجم — ضربان per-node و تأیید ریسک ایمپورت

### ریسک تایم‌استمپ ایمپورت — بسته است، و بهتر از آنچه فکر می‌کردم

هشدار قبلی من دیگر معتبر نیست. `db/crud/violations.py` و `db/migrate_from_json.py`
(همان دو فایل کامیت‌نشده) دقیقاً همین را اصلاح کرده‌اند:

- `ViolationHistoryCRUD.add` پارامتر `timestamp` گرفته و
  [violations.py:39](db/crud/violations.py:39) `recorded_at = time.time() if timestamp is None else float(timestamp)`
  — صداکننده‌های زنده مثل قبل «حالا» می‌گیرند، ایمپورتر تایم‌استمپ اصلی را می‌دهد.
- [migrate_from_json.py:250](db/migrate_from_json.py:250) واقعاً `timestamp=original` را پاس می‌دهد،
  و رکورد بدون تایم‌استمپ قابل‌استفاده **رد** می‌شود نه اینکه تایم‌استمپ برایش ساخته شود
  ([:235-243](db/migrate_from_json.py:235)).
- **مهم‌ترین گارد:** اگر جدول از قبل ردیف داشته باشد کل ایمپورت no-op است
  ([:223-229](db/migrate_from_json.py:223)). روی نصب شما ۲٬۷۸۴ ردیف هست، پس این تابع
  بی‌درنگ صفر برمی‌گرداند و هیچ‌چیز دست نمی‌خورد.
- و یک فاجعه‌ی نهفته‌ی دیگر هم بسته شده: `--legacy-only` تابع `import_legacy_stores` را صدا
  می‌زند که **عمداً config را دست نمی‌زند** ([:292-309](db/migrate_from_json.py:292)). اگر
  `main()` را در هر بوت اجرا می‌کرد، `config.json` قدیمی روی تنظیمات زنده — کردنشیال پنل،
  سقف‌ها، فیلتر گروه، پله‌های تنبیه — بازنویسی می‌شد.

### ضربان per-node اضافه شد

`NODE_LAST_EVENT` در [shared_state.py](utils/shared_state.py) نگاشت `{node_id: timestamp}` است.
بدون قفل و عمداً: در هر خط لاگ نوشته می‌شود و انتساب یک float در دیکشنری زیر GIL اتمیک است؛
قفل گرفتن به‌ازای هر خط از چیزی که محافظت می‌کند گران‌تر بود، و بستنش به `ACTIVE_USERS_LOCK`
ورود داده را پشت اسنپ‌شات‌گیری سریالایز می‌کرد.

نقاط اتصال: seed بعد از `"✅ Connected"`، ثبت روی **هر خط** استریم (نه فقط خطوط `data:`
پارس‌شده — چون سؤال «استریم زنده است؟» است نه «کاربری وصل شد؟»؛ نودی که همه‌ی خطوطش فیلتر
می‌شود نباید مرده خوانده شود)، حذف در `handle_cancel` وقتی پنل نود را disconnected اعلام
می‌کند، و `clear_node_events()` در بازسازی دوساعته.

خواننده در [check_usage.py](utils/check_usage.py) کنار همان `counters_are_trustworthy` است.
دو سیگنال جدا، چون دو خرابی مختلف را می‌گیرند: اسنپ‌شات خالی «همه‌چیز مرده» را، و ضربان
per-node خرابی **جزئی** را — که اسنپ‌شات خالی هرگز نمی‌بیندش، چون یک نود مرده از چهل‌ونه تا
باز هم نمونه‌ی سالمی به‌جا می‌گذارد و فقط کاربران همان نود «قطع‌شده» به نظر می‌رسند.

**تصمیم طراحی که باید بدانید:** یک نود که مرده می‌ماند در `NODE_LAST_EVENT` **باقی می‌ماند**
با تایم‌استمپ کهنه. حذفش وسوسه‌کننده بود ولی غلط است: نودی که از نگاشت بیرون برود ناوگان را
کامل نشان می‌دهد و ما را برمی‌گرداند سر خانه‌ی اول. فقط وقتی حذف می‌شود که پنل رسماً بگوید
نود disconnected است.

**بده‌بستان صادقانه:** پنجره‌ی کهنگی `max(120s, check_interval × 2)` است — روی
`check_interval=300` یعنی ۶۰۰ ثانیه. وقتی گارد فعال شود هیچ شمارنده‌ای پاک نمی‌شود، و این
یعنی کاربری که واقعاً نرمال شده کمی بیشتر زیر مانیتورینگ می‌ماند. برای کاربری که مرتب
بالا و پایین نوسان می‌کند، این نظری می‌تواند رسیدن به اسکن سوم را تسریع کند. پنجره را از
همین رو دست‌ودل‌بازانه گرفتم و رخدادش ERROR لاگ می‌شود با شماره‌ی نودهای ساکت. خرابی‌ای که
می‌بندد — «متخلف واقعی هرگز بن نمی‌شود» — همان است که در پروداکشن دیده شد.

---

## دور ششم — آدیت دستورهای تلگرام، و بستن پرونده

آدیت مستقل `HELP_TEXT` در برابر ثبت‌های `main.py`:

- **هیچ دستوری که تبلیغ شده باشد، ثبت‌نشده نیست.** هر ۳۶ دستور `HELP_TEXT` و `/help` در
  `START_MESSAGE` به یک ثبت واقعی می‌رسند. نگرانی قبلی من درباره‌ی `/show_enhanced_details`
  بی‌مورد بود: آن نام در `constants.py` وجود ندارد، پس تبلیغ نشده. Enhanced Details فقط
  دکمه‌ای است.
- **هیچ دکمه‌ی قابل‌فشاری خراب نیست.** هر callback_data که از یک کیبورد قابل‌دسترس صادر
  می‌شود، در `CALLBACK_ROUTES` مسیر دارد.
- `handle_enhanced_menu_callback` هم ایمپورت شده (main.py:124) و هم در `CALLBACK_ROUTES`
  هست (main.py:498)، و دکمه‌ی Enhanced Details زیرمنو را باز می‌کند نه اینکه تنظیم را
  بی‌واسطه عوض کند. باگی که در کامنت `constants.py:111-115` توصیف شده در کد فعلی برطرف است.
- شمارش نهایی: ۴۵ `CommandHandler` (۳۴ سطح‌بالا، ۱۰ ورودی مکالمه، ۱ fallback) و
  ۱۰ `ConversationHandler`.

**هفت دستور ثبت‌شده ولی نامستند بودند** و به `HELP_TEXT` اضافه شدند:
`punishment_set_steps`، `group_filter_add`، `group_filter_remove`، `admin_filter_add`،
`admin_filter_remove`، `users_by_node`، `users_by_protocol`. عمداً `cancel` اضافه نشد —
آن فقط fallback داخل مکالمه‌ی `migrate_backup` است و تبلیغ عمومی‌اش همان دروغی می‌شد که
داشتیم پاکش می‌کردیم.

یازده هندلر غیرقابل‌دسترس در `settings_intervals.py` و `settings_display.py` از قبل در
docstring خودشان با ذکر دلیل و مرجع تأیید مستند شده‌اند؛ کد مرده‌اند نه UI شکسته، و حذفشان
ریسک بی‌دلیل است چون `settings.py` بازصادرشان می‌کند.

اگر در قدم ۴ عبارت `Migrations skipped` دیدید، مایگریشن **شکست خورده** است نه اینکه لازم نبوده.

---

### B3 — از قبل اصلاح شده بود

`start.sh` حالا حالت دیتابیس را پروب می‌کند ([start.sh:192-214](start.sh:192)) و چهار خروجی
`FRESH`/`VERSIONED`/`NEEDS_STAMP`/`NOT_SQLITE` می‌دهد؛ روی `NEEDS_STAMP` اول
`alembic stamp head` می‌زند ([:219](start.sh:219))، حالت ناشناخته را کشنده می‌گیرد
([:228](start.sh:228))، و شکست یا تایم‌اوت مایگریشن حالا با چاپ کامل خروجی خط‌به‌خط
`exit 1` می‌کند ([:240](start.sh:240) و [:246](start.sh:246)). جمله‌ی گمراه‌کننده‌ی
«Migrations skipped (may already be applied)» حذف شده.

### B4 — اصلاح شد: مایگریشن ۰۰۷

فایل [007_ip_history_unique.py](db/migrations/versions/007_ip_history_unique.py) اضافه شد،
با `down_revision = '006_drop_patterns'`. تأیید شد که تنها ۰۰۷ موجود است، پس یک head داریم.

منطقش: اگر جدول نبود یا یگانگی از قبل تأمین شده بود بی‌صدا برمی‌گردد؛ وگرنه اول جفت‌های
تکراری را با نگه‌داشتن بزرگ‌ترین `id` هر جفت حذف می‌کند (که آخرین `last_seen` را دارد) و
بعد ایندکس یونیک را می‌سازد. روی دیتابیس این اپراتور `ip_history` صفر ردیف دارد پس آن
`DELETE` بی‌اثر است.

**یک تله که در نسخه‌ی اول همین مایگریشن داشتم و اصلاحش کردم:** بررسی «از قبل یونیک هست؟»
اولش فقط `get_indexes()` را نگاه می‌کرد. اما `create_all` محدودیت مدل را به‌صورت
constraint داخل `CREATE TABLE` می‌سازد و SQLite آن را با ایندکس خودکار
`sqlite_autoindex_*` پشتیبانی می‌کند — و SQLAlchemy آن‌ها را از `get_indexes()` **فیلتر**
می‌کند. پس روی یک دیتابیس ساخته‌شده با `create_all` گارد رد می‌شد و یک ایندکس یونیک
دوم و زائد ساخته می‌شد. حالا `get_unique_constraints()` هم بررسی می‌شود.

### دستورهای تلگرامی که وجود ندارند

`main.py` سی‌و‌چهار `CommandHandler` و ده `ConversationHandler` دارد و هیچ‌کدام
`set_check_interval`، `set_time_to_active_users` یا `set_country_code` نیستند — تأییدشده.
در حالی که [limiter.py:172-176](limiter.py:172) وقتی کانفیگ ناقص است دقیقاً دو تای اول را
به اپراتور توصیه می‌کند. `/create_config` و `/set_general_limit_number` وجود دارند.
شما این مقادیر را از `.env` می‌دهید پس عملاً کار می‌کند؛ برای انتشار عمومی یا آن دستورها
باید ثبت شوند یا متن پیام اصلاح شود.

### دکمه‌های مرده — شدت پایین

یازده هندلر در `settings_intervals.py`، `settings_display.py` و `settings_common.py`
غیرقابل‌دسترس‌اند، اما **هیچ دکمه‌ی قابل‌فشاری خراب نیست**: `CallbackData` حتی ثابتی برای آن
چهار منو ندارد، و `create_settings_menu_keyboard` هیچ راهی به آن صفحه‌ها نمی‌دهد. یعنی کد
مرده است نه UI شکسته. مورد جانبی: `handle_enhanced_menu_callback` در main.py ایمپورت شده
ولی در `CALLBACK_ROUTES` نیست، و دکمه‌ی «Enhanced Details» مستقیم `ENHANCED_ON` می‌فرستد،
پس زیرمنوی toggle رد می‌شود و تنظیم بی‌واسطه روشن می‌شود.

---

## اصلاحات دور دوم

### `init_db()` حالا کشنده است — [limiter.py](limiter.py)

خطا با `main_logger.critical` ثبت و بعد `raise SystemExit(1)` می‌شود. مسیر خروج تمیز است:
`SystemExit` از `BaseException` می‌آید نه `Exception`، پس `except Exception` پایین‌تر آن را
نمی‌گیرد؛ `finally: await cleanup_resources()` اجرا می‌شود و هندلر `except SystemExit`
دوباره raise می‌کند. در آن نقطه هنوز هیچ تسکی ساخته نشده، پس یتیمی باقی نمی‌ماند.

### توکن بات دیگر در لاگ نیست — [run_telegram.py](run_telegram.py)

سه خط دیباگ که `type`، `repr(token[:20])` و `bool` را چاپ می‌کردند با یک خط
`Bot token configured: True/False` جایگزین شد، و خط INFO حالا فقط بخش id را نشان می‌دهد:
`(id=..., secret redacted)`. توکن ساختار `<۱۰ رقم>:<۳۵ کاراکتر راز>` دارد، پس `token[:20]`
نه کاراکتر از راز را لو می‌داد و `token[:15]` چهار کاراکتر — و دومی در سطح INFO بود، یعنی
در هر لاگ عادی که جابه‌جا می‌شود.

### شکست استارت بات دیگر بی‌صدا نیست — [run_telegram.py](run_telegram.py)

نبود توکن حالا `RuntimeError` می‌دهد و هر دو بلوک `except` بعد از لاگ `raise` می‌کنند.
قبلاً کوروتین نرمال تمام می‌شد، پس `_log_bot_task_failure` در `limiter.py` هیچ استثنایی
نمی‌دید و اپراتور لاگ سالم با بات مرده داشت. چون این تسک **بیرون** TaskGroup است،
raise کردن اجرای بن را متوقف نمی‌کند — فقط خرابی را دیدنی می‌کند و واچ‌داگ هر ۶۰ ثانیه
تلاش می‌کند. شاخه‌ی «already running» همچنان بی‌صدا `return` می‌کند.

**عمداً برخلاف پیشنهاد ایجنت:** تسک بات را داخل `_supervise` نبردم. `run_telegram_bot()`
بعد از شروع polling **نرمال برمی‌گردد**، پس `_supervise` هر بار
`⚠️ returned unexpectedly, restarting` می‌زد و هر ۱۰ ثانیه دوباره صدا می‌زد — یک حلقه‌ی
پر‌سر‌و‌صدا به‌جای یک اصلاح.

### مسابقه‌ی واچ‌داگ و ۴۰۹ بسته شد — [run_telegram.py](run_telegram.py)

`application.initialize()`، `application.start()` و `updater.start_polling(...)` هر سه در
`async with asyncio.timeout(30)` رفتند. `_telegram_watchdog` شصت ثانیه بعد از ثبت بیدار
می‌شود و اگر `updater.running` هنوز False باشد یک `start_polling` دوم روی همان updater
صادر می‌کند — دو حلقه‌ی `getUpdates` که تلگرام با HTTP 409 جواب می‌دهد. با سقف ۳۰ ثانیه،
هیچ هنگی نمی‌تواند از اولین بیداری واچ‌داگ عبور کند. خاموشی از قبل ایمن بود: واچ‌داگ فرزند
TaskGroup است و قبل از `cleanup_resources()` کنسل می‌شود.







